# src

実装。**技術スタック未確定のため空。**

フェーズ 5（実装）に入るまでファイルを置かない。技術検証のコードは `../scratch/` へ。

構成は基本設計（`docs/20_basic_design/architecture.md`）で決めるが、
少なくとも「環境に依存しない純粋ロジック」と「環境に触る層」は分ける。
純粋ロジック側が `Math.random()` / `Date.now()` を直接呼ばないことが、
ハーネスが成立する前提になる。
