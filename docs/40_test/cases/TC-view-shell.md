# TC-120〜139 — 表示層とライフサイクル、NFR

対象: [MOD-View](../../30_detailed_design/MOD-View.md) /
[MOD-Tutorial](../../30_detailed_design/MOD-Tutorial.md) / [MOD-Shell](../../30_detailed_design/MOD-Shell.md)

## 表示（MOD-View）

| TC | 対象 REQ | 種別 | 手順 | 期待値 |
| --- | --- | --- | --- | --- |
| TC-120 | REQ-001 | e2e | 首を可動範囲いっぱいに振る | **天井・親の顔・親の手が視界に入りうる** |
| TC-121 | REQ-006 / D-11 | e2e | 視線を 3 対象それぞれに向ける | **向けた対象の情報だけが出る。**他は伏せられる |
| TC-122 | REQ-006 / D-11 | e2e | 首の可動範囲を全走査 | **3 対象が同時に視界へ入る角度が存在しない** |
| TC-123 | **REQ-004** | e2e | 目を閉じる | **寝室の像が完全に消える。**画素を検査し、窓の領域以外に寝室由来の輝度差が無い |
| TC-124 | REQ-004 | e2e | 閉眼中の描画を検査 | **半透明の重ねが使われていない**（alpha を混ぜると寝室が透ける） |
| TC-125 | REQ-016 | e2e | 寝たふりが成功する seed と失敗する seed | **閉眼中の画面が同一**（成否が漏れない） |
| TC-126 | REQ-044 | e2e | 全画面 | **数値・ゲージ・アイコンが 0 件** |
| TC-127 | REQ-044 | e2e | 各段階を**グレースケール化**して比較 | **明度差を消しても段階が区別できる**（輪郭・姿勢・周期で冗長化） |
| TC-128 | **NFR-007** | e2e | 全画面を撮影 | **画素の相対輝度の平均が 0.1 以下** |
| TC-129 | REQ-005 | e2e | 画面の下半分だけで操作 | **全操作が到達できる。**同時接触を要求しない |
| TC-130 | — | unit | `Stages` に -1 を渡す | **その情報を出さない**（推測して埋めない） |

## チュートリアル（MOD-Tutorial）

| TC | 対象 REQ | 手順 | 期待値 |
| --- | --- | --- | --- |
| TC-131 | REQ-034 | 3 段を通す | 覚醒度・元気・寝たふりを**操作しながら**達成できる |
| TC-132 | REQ-034 / D-09 | チュートリアルを開始 | **日付シードを使わない。**日を変えても同じ盤面 |
| TC-133 | REQ-031 / 048 | チュートリアルを完了 | **`Today.PlayCount` が増えない** |
| TC-134 | screens.md 3 節 | 戻る操作 | **受け付けない**（完了以外の出口が無い） |

## ライフサイクル（MOD-Shell）

| TC | 対象 REQ | 手順 | 期待値 |
| --- | --- | --- | --- |
| TC-135 | screens.md 2 節 | T-01〜T-13 を全て実行 | **遷移表と 1 対 1。**未定義の遷移が発生しない |
| TC-136 | screens.md 3 節 | 全画面で戻る操作 | **行き先が定義どおり。**行き止まりが 0 件 |
| TC-137 | REQ-009 / 010 | フォーカスを外す | **時間が止まり、その tick の状態が保存される** |
| TC-138 | REQ-010 | プロセスを破棄して再起動 | **続きから再開できる** |
| TC-139 | REQ-037 | 進行中に放棄してやり直す | **`PlayCount` が増える**（放棄も数える） |
| TC-140 | D-10 | 進行中のプレイがある状態で起動 | **自動で `SCR-Night` に入らない。**`SCR-Home` で選ばせる |
| TC-141 | D-08 | 正午をまたいで中断・再開 | **完走できる。**前の日の最高成績には入らない |
| TC-142 | **REQ-051** | 1 tick の呼び出しを検査 | **`Sim.Advance` → `Score.Apply` → `NightEnd.Evaluate` の順**（順序を間違えると TC-080 が落ちる） |
| TC-143 | REQ-033 | `Device` を壊して起動 | **起動できる。**`SCR-Recover` → `SCR-Tutorial` |
| TC-144 | REQ-033 | 盤面仕様の版を変えて起動 | `Today` + `Run` を捨てて `SCR-Home`（**`SCR-Recover` を通さない**） |
| TC-157 | REQ-024 | 同じ日に 5 回続けて遊ぶ | **5 回とも遊べる。**`PlayCount` が 5 になり、盤面は毎回同じ |
| TC-158 | **REQ-022** | 遷移表 T-01〜T-13 と全画面の操作を走査 | **過去の日付を選ぶ入口が存在しない**（端末の時計操作は防がない） |

## 非機能（実機）

| TC | 対象 NFR | 手順 | 期待値 |
| --- | --- | --- | --- |
| TC-145 | NFR-001 | 実機で 1 プレイ通す | 最後まで通る |
| TC-146 | NFR-002 | 機内モードで全機能 | **動く** |
| TC-147 | NFR-003 | ビルドを検査 | 要求権限 0 件、**通信を行う外部 SDK が 0 件** |
| TC-148 | NFR-004 | 10 シード × 同一入力列を 10 回 | 状態列の差分 0（= TC-021） |
| TC-149 | NFR-006 | 「一度も行動しない」「最短で夜を終わらせる」入力列 | **どちらも実時間 5 分以内** |
| TC-150 | **NFR-008** | 実機でコールドスタート 5 回 | **中央値 3 秒以内**で操作を受け付ける |
| TC-151 | **NFR-009** | 本人と配偶者が別の日に 3 回ずつ通す | 酔いの自己申告が**いずれも下位 2 段階** |

## 暫定（ADR 承認待ち）

| TC | 対象 REQ | 期待値 |
| --- | --- | --- |
| TC-152 | REQ-060（暫定） | 短く離した場合と長く引っ張った場合で、上昇量・元気の消費・対処の確率が**いずれも単調に増える** |
| TC-153 | REQ-060（暫定） | **溜め続けることに代償がある**（元気が減る / 対処が始まると溜めが飛ぶ） |
| TC-154 | REQ-061（暫定） | **寝入りばな**の内と外で上昇量が異なる |
| TC-155 | REQ-061（暫定） | **予兆から寝入りばなまでの間隔が、最大まで溜めるのに要る時間以上**（間に合う） |
| TC-156 | REQ-061（暫定） | **顔を見ていないとき予兆が表示されない** |

| TC-159 | REQ-062（暫定） | **同じ入力列から常に同じ診断**が出る（近傍マッチが決定論） |
| TC-160 | REQ-062（暫定） | **立ち回りの違う 7 方針から、違う診断**が出る（24 シードで、同じ診断に偏らない） |
| TC-161 | REQ-062（暫定） | 診断に**盤面の答えが含まれない**（REQ-028） |
| TC-162 | REQ-062（暫定） | 7 軸の**相関が 0.6 を超える組が無い**（1 軸に潰れていない） |
| TC-163 | REQ-062（暫定） | 合計が閾値未満のとき **DX-41 が固定で返る**（割合ベクトルが定義できない場合） |

**ADR-0015 が棄却されたら TC-152〜156 を、ADR-0016 が棄却されたら TC-159〜163 を削除する。**
