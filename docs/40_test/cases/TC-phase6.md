# TC-166〜177 — 遊べる形に繋ぐときに足したもの（フェーズ 6）

対象: [MOD-Result](../../30_detailed_design/MOD-Result.md)（実況の素材・診断の濃さ） /
[MOD-Present](../../30_detailed_design/MOD-Present.md)（状態 → 見せ方） /
[MOD-Storage](../../30_detailed_design/MOD-Storage.md)（実ファイルでの往復）

> **2026-09-13 追加。**Unity 側で「ホーム → 夜 → 結果 → 共有」を繋いだとき、
> 規則として切り出せるものが 3 つ出た。**テストを先に書き、赤を確かめてから実装した**（test_first.md）。
> 既存の TC は 1 件も書き換えていない。
>
> **TC-176 / 177 は既存実装の欠陥を拾った。**`FileStorage` が入れ子の値（山札・慣れ・診断）を書き戻せず、
> その日の最高成績も保存していなかった。TC-108 は `MemoryStorage` で見ていたので気づけなかった。

## 実況の素材と診断の濃さ（MOD-Result。`tests/unit/CommentaryTests.cs`）

| TC | 対象 REQ | 前提 | 手順 | 期待値 |
| --- | --- | --- | --- | --- |
| TC-166 | REQ-026 / 054 | 夜の状態の前後 | `Commentary.Observe` | 得点が 0→1 になった瞬間・寝たふりの着地（`Settling`→`Grace`）・夜の終わりが**種類ごとに 1 回だけ**積まれる。**`Feint` の満了は「通った」と積まない**。何も起きない tick では積まない |
| TC-167 | REQ-026 / 020 | 5 種の素材 | `Commentary.Pick` | **3 件以内**。終わり方が残る。起きた順に並ぶ。**入力の並びで結果が変わらない** |
| TC-168 | REQ-032 / 033 | 素材の列 | `Encode` → `Decode` | 往復で一致する。**壊れた文字列でも例外を投げない** |
| TC-169 | REQ-062 | `Tuning` の閾値の前後 | `StrengthOf` / `Title` | 閾値未満は「うっすら」、上限以上は「どっぷり」、並は修飾なし。**`DX-41` には付けない** |

## 状態 → 見せ方（MOD-Present。`tests/harness/PresentTests.cs`）

| TC | 対象 REQ | 前提 | 手順 | 期待値 |
| --- | --- | --- | --- | --- |
| TC-170 | **REQ-004 / 016** / ADR-0014 | 6 シードの夜で通った閉眼中の状態 | 覚醒度・山札・寝返り・空腹・もう一人・予告・元気を変える | **見せ方が変わらない**（閉眼中に出るのは窓と「運ばれている」だけ） |
| TC-171 | **REQ-016** / ADR-0017 / 0022 | 10 シードの夜で通った `Settling` / `Feint` | 親の状態だけを入れ替える | **見せ方が同じ。**`Body = Carry` かつ視点が動く |
| TC-172 | REQ-030 | 親が `Up` | `Of` | 母が映らず、電気が点く |
| TC-173 | REQ-006 / 013 / 046 | 対処中 / 予告中 / トントン中 | 札種・山札の段階を変える | 対処 4 種が区別できる。予告 4 種が手から分かる。トントンの手つきが 3 段階で変わる |
| TC-174 | REQ-044 / 045 | 開眼・寝入りばなの予兆の外 | 覚醒度を段階の境界に置く | 4 段階が別の姿勢。**寝返り中は覚醒度で姿勢が変わらない** |
| TC-175 | REQ-055 | スマホ・オムツの出来事を置いた盤面 | 発生直後と後 | スマホが一瞬光る（光り続けない）。鼻をひくつかせる。もう一人が現れる。空腹で視界が沈む |

## 実ファイルでの往復（MOD-Storage。`tests/harness/FileStorageTests.cs`）

| TC | 対象 REQ | 前提 | 手順 | 期待値 |
| --- | --- | --- | --- | --- |
| TC-176 | **REQ-010 / 020 / 032** | 慣れ・診断が動いた夜の途中 | `FileStorage` に保存 → 別インスタンスで読む → 続きを 600 tick | **状態が一致し、続きの状態列も一致する** |
| TC-177 | REQ-025 / 032 / 033 | 最高成績あり / なし / 範囲外の値 | 保存 → 読む | 最高成績が往復する。無い日も読める。**範囲外は `Corrupt`** |

## 首振りの初期値と相対ドラッグ（MOD-Input。`tests/harness/InputTests.cs`）

**2026-09-13 に Mac ビルドで「夜の初期視線が母の顔になっている」ことを見つけて切り出した。**
`PointerInputSource` は最初のドラッグまで (0, 0) を返し、首の向きを指の絶対位置で決めていた。

| TC | 対象 REQ | 前提 | 手順 | 期待値 |
| --- | --- | --- | --- | --- |
| TC-178 | REQ-001 / 002 / screens.md 4.2.1 | 初期視線を注入した入力源 | 触る前 / 触れただけ | **注入した初期視線のまま**（触れただけで跳ばない） |
| TC-179 | REQ-002 / MOD-Input INP-02 | 同上 | 端から触って少し動かす → 離す → 反対側から触り直す | **動かした分だけ向きが変わる。**持ち替えても向きが跳ばない |

## バランス値との関係

**期待値に具体値を書いていない**（ADR-0012）。濃さの境界は `Tuning` から読み、
見せ方の値（`DozeWarnTicks` など）は「その区間の外の tick を選ぶ」ためにだけ使う。
