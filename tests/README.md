# tests

方針は [../docs/00_process/test_first.md](../docs/00_process/test_first.md)。

| ディレクトリ | 対象 | 依存してよいもの |
| --- | --- | --- |
| `unit/` | 純粋ロジック単体 | src の内部関数 |
| `harness/` | 入力列を流し込んだ通しの振る舞い | ハーネス経由の公開 IF のみ |
| `e2e/` | 実際の実行環境での起動・操作 | 実行環境 |

**実装より先に書く。書いた直後は全部落ちているのが正しい。**
実装フェーズでテストを書き換えて通すのは禁止。
